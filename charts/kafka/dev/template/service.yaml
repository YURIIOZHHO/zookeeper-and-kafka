apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
    - name: {{ .Release.Name }}-listener
      port: {{ .Values.listeners.client.containerPort }}
  clusterIP: None
  selector:
    app: {{ .Release.Name }}