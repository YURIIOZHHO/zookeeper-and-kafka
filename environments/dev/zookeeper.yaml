apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name     : zookeeper
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name     : in-cluster
    namespace: kafkakeeper
  sources:

    - repoURL: https://github.com/YURIIOZHHO/zookeeper-and-kafka.git
      targetRevision: main
      ref: values

    - chart: zookeeper
      repoURL: https://charts.bitnami.com/bitnami
      targetRevision: 13.8.*
      helm:
        releaseName: zookeeper
        valueFiles:
          - $values/charts/zookeeper/dev/values_dev.yaml

  project: default
  syncPolicy:
    automated:
      prune   : true
      selfHeal: true